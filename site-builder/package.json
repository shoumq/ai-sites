{
  "name": "site-builder",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "description": "HTTP-обёртка над site-renderer: принимает SiteSchema от бэкенда, запускает `nuxi generate` и копирует статическую сборку в /builds/{subdomain}/{build_id}/.",
  "main": "server.mjs",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "async-mutex": "^0.5.0",
    "express": "^5.2.1"
  }
}
